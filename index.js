/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StartContainer from './components/StartContainer';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StartContainer);
